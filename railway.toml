# Railway deployment config
# https://docs.railway.com/

[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn server.main:app --host 0.0.0.0 --port ${PORT:-8000}"

[service]
name = "aigate"
